{
  "framework": "nextjs",
  "buildCommand": "unset NODE_ENV && npx drizzle-kit push && npx tsx src/db/seed.ts && next build",
  "installCommand": "unset NODE_ENV && npm install",
  "outputDirectory": ".next"
}
